// Write a JavaScript script that updates the text color of the <header> element to red (#FF0000). Must use document.querySelector.

const header = document.querySelector('header');
header.style.color = 'red';
